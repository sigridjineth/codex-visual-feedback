# ax-tree

Dump AX tree JSON for a target app process.

```bash
codex-visual-loop ax-tree --process "Promptlight" --depth 3 --json
```

Common options:

- `--depth <n>` traversal depth (default: `3`)
- `--out <path>` output JSON file path
- `--json` print AX payload to stdout
